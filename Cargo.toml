[package]
name = "san"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
crossterm = "0.28"
fastrand = "2"

[profile.dev]
opt-level = 2

[profile.release]
opt-level = 3
lto = true
